Source: https://craftdex.net/commands
Title: Minecraft Commands (26.2) — Cheats &amp; Operators — Craftdex
Category: mechanics
Fetched: 2026-07-20T16:48:09.144Z

============================================================

All World Entity Player Items Scripting Data Audio/Visual Messaging Admin Info Mode/Time Showing 88 of 88 commands. /execute scripting Run a command as another entity, at another position, conditionally, or storing the result. /execute [<modifier>...] [<condition>...] [store <result>] run <command> /scoreboard data Manage objectives + per-player score values for redstone, datapacks, and minigames. /scoreboard objectives <add|remove|list|setdisplay|modify> /scoreboard players <add|set|get|operation|reset|list|display> /data data Read, write, merge, and remove NBT on blocks, entities, and storage. /data <get|merge|modify|remove> (block <pos>|entity <target>|storage <id>) [<path>] [<scale>] /loot items Spawn / give / insert / replace items by simulating a loot table (chest, fishing, mob kill, block break). /loot <give|insert|spawn|replace> <target> <source> [<source-args>] /fill world Fill a cuboid region with a block. Replace, hollow, outline, destroy, or keep modes. /fill <from> <to> <block> [destroy|hollow|keep|outline|replace [filter]] /clone world Copy or move a region of blocks to another location, optionally across dimensions. /clone <begin> <end> <destination> [replace|masked|filtered <block>] [normal|force|move] /clone from <dim> <begin> <end> to <dim> <dest> ... /particle audio visual Spawn visual particles at a position with offset, speed, count, and visibility settings. /particle <name> [<pos>] [<delta>] [<speed>] [<count>] [force|normal] [<viewers>] /attribute entity Get or set entity attributes (max health, attack damage, movement speed, etc). /attribute <target> <attribute> (get|base get|base set <value>|base reset|modifier add|remove|value get) [...] /bossbar audio visual Create and control custom bossbars (top-of-screen progress meters) for players. /bossbar (add|get|list|remove|set) <id> [...] /item items Replace or modify items in any inventory slot of a block or entity. /item replace (block|entity) <target> <slot> with <item> [count] /item replace ... from ... <slot> /item modify ... <modifier> /give items Give items (with optional component data) to one or more players. /give <player> <item>[<components>] [count] /summon entity Spawn an entity at a position with optional NBT modifications. /summon <entity> [<pos>] [<nbt>] /teleport (/tp) player Move entities to a position or to another entity. Aliases: /tp. /teleport <target> (<destination>|<entity>) [<rotation>|facing <pos>|facing entity <entity>] /gamemode mode time Set the game mode (survival/creative/adventure/spectator) for one or more players. /gamemode <survival|creative|adventure|spectator> [<targets>] /time mode time Set or query the in-game time. Day/night skipping in one command. /time set (day|noon|night|midnight|<value>) /time add <value> /time query (day|daytime|gametime) /weather mode time Set the weather to clear, rain, or thunder for a number of seconds. /weather (clear|rain|thunder) [<duration>] /kill entity Instantly deal void damage to the targets (can kill players bypassing armor). /kill [<targets>] /effect entity Apply or clear status effects (potion effects) on entities. /effect give <target> <effect> [<seconds>] [<amplifier>] [hideParticles] /effect clear <target> [<effect>] /enchant items Add an enchantment to the target's currently held item (respects compatibility rules). /enchant <target> <enchantment> [<level>] /experience (/xp) player Add or set XP points or levels on players. Aliases: /xp. /experience (add|set|query) <target> <amount> [points|levels] /tellraw messaging Send a JSON-formatted message to players (colors, hover/click events, scores, selectors). /tellraw <target> <json> /title audio visual Display a large centered title (or subtitle / actionbar) to a player. /title <target> (title|subtitle|actionbar|clear|reset|times <fadeIn> <stay> <fadeOut>) [<json>] /locate world Find the nearest structure, biome, or POI and print its coordinates. /locate (structure|biome|poi) <id> /gamerule admin Get or set a world rule (~50 toggles like keepInventory, doDaylightCycle, mobGriefing, etc). /gamerule <rule> [<value>] /playsound audio visual Play a sound effect at a position for selected players, with category, volume, pitch. /playsound <sound> <category> <target> [<pos>] [<volume>] [<pitch>] [<minVolume>] /advancement player Grant or revoke advancements (and individual criteria) for players. /advancement (grant|revoke) <target> (only|until|from|through|everything) [<advancement>] [<criterion>] /datapack admin Enable, disable, or list datapacks loaded in the world. /datapack (enable|disable|list) [<name>] [first|last|before|after <other>] /function scripting Execute a .mcfunction file from a datapack. /function <namespace>:<function> /schedule scripting Run a function after a delay (in ticks/seconds/days). /schedule function <function> <time> [append|replace] /schedule clear <function> /tag data Add, remove, or list custom string tags on entities (used as redstone-friendly flags). /tag <target> (add|remove|list) [<name>] /team data Manage colored teams (chat color, friendly fire, name visibility, collision rules). /team (add|empty|join|leave|list|modify|remove) [...] /advancement player (See main /advancement entry.) /advancement ... /ban admin Ban a player from the server. /ban <player> [<reason>] /ban-ip admin Ban an IP address from the server. /ban-ip <address|player> [<reason>] /banlist admin List banned players or IPs. /banlist [players|ips] /clear items Remove items from a player's inventory (filter by id and components). /clear [<targets>] [<item>] [<maxCount>] /damage entity Deal a typed damage amount to entities. /damage <target> <amount> [<type>] [at <pos>|by <entity> [from <entity>]] /debug info Start/stop a debug profiling session (creates a profile file). /debug (start|stop|function <name>) /defaultgamemode mode time Set the default gamemode new players join with. /defaultgamemode <mode> /deop admin Revoke operator status from a player. /deop <player> /dialog audio visual Open a registered dialog UI for the target player (1.21.6+). /dialog show <target> <dialog> /difficulty mode time Set the world difficulty (peaceful, easy, normal, hard). /difficulty <peaceful|easy|normal|hard> /fillbiome world Replace the biome in a cuboid region. /fillbiome <from> <to> <biome> [replace <filter>] /forceload world Force-load chunks so they tick even without a player nearby. /forceload (add|remove|query) <from> [<to>] /help info List available commands or show usage for a specific command. /help [<command>] /jfr info Start/stop a Java Flight Recorder profiling session. /jfr (start|stop) /kick admin Kick a player off the server (they can rejoin). /kick <player> [<reason>] /list info List the players currently online. /list [uuids] /me messaging Send a third-person action message (e.g. * Steve waves). /me <action> /msg (/tell, /w) messaging Send a private message to one or more players. Aliases: /tell, /w. /msg <target> <message> /op admin Grant operator status to a player. /op <player> /pardon admin Remove a player from the ban list. /pardon <player> /pardon-ip admin Remove an IP from the ban list. /pardon-ip <address> /perf info Run a 10-second performance profiler (server-side). /perf (start|stop) /place world Place a structure, jigsaw, template, or feature at the given position. /place (feature|jigsaw|structure|template) <id> [...] /publish admin Open singleplayer to LAN multiplayer. /publish [<allowCheats>] [<gamemode>] [<port>] /random scripting Roll a random integer (or use shared/sequential generators). /random (value|roll|reset) <range> [<sequence>] /recipe items Grant or revoke recipes (unlock the recipe book entries) for players. /recipe (give|take) <target> (* | <recipe>) /reload admin Reload datapacks (loot tables, recipes, advancements, functions, tags) without restarting. /reload /return scripting Exit a function early with a return value. /return <value> /return run <command> /return fail /ride entity Mount or dismount entities programmatically. /ride <target> (mount <vehicle>|dismount) /rotate entity Rotate an entity to a yaw/pitch or to face another entity. /rotate <target> (<rotation>|facing <pos>|facing entity <entity> [<anchor>]) /save-all admin Save the world immediately to disk. /save-all [flush] /save-off admin Disable automatic world saving (for backups). /save-off /save-on admin Re-enable automatic world saving. /save-on /say messaging Broadcast a message to chat from the executor (shown as [Server] or [PlayerName]). /say <message> /seed info Print the world seed. /seed /setblock world Place a single block at a position. Supports replace, destroy, keep modes. /setblock <pos> <block> [destroy|keep|replace] /setidletimeout admin Set the auto-kick idle timeout (in minutes). /setidletimeout <minutes> /setworldspawn world Set the world spawn point (default respawn for new players). /setworldspawn [<pos>] [<angle>] /spawnpoint player Set personal respawn point for selected players. /spawnpoint [<targets>] [<pos>] [<angle>] /spectate player Make a spectator-mode player follow another entity (third-person). /spectate [<target>] [<player>] /spreadplayers player Randomly scatter entities within an area (with min spread). /spreadplayers <center> <spreadDistance> <maxRange> [under <maxHeight>] <respectTeams> <targets> /stop admin Stop the dedicated server. /stop /stopsound audio visual Stop a sound currently playing for selected players. /stopsound <target> [<source>] [<sound>] /swing entity Force the target's hand-swing animation. /swing <target> [mainhand|offhand] /teammsg (/tm) messaging Send a chat message visible only to your team. Aliases: /tm. /teammsg <message> /test scripting Run datapack-defined gametest scenarios (developer feature). /test (run|runall|runfailed|runthese|runthis|locate|pos|stop|verify) [...] /tick mode time Query, freeze, or fast-forward server tick rate (1.20.3+). /tick (query|rate <ticks/sec>|freeze|unfreeze|step [<ticks>]|sprint <ticks>) /tp player Alias of /teleport. See /teleport for full syntax. /tp <target> ... /transfer player Transfer players to another server (Realms / multi-server setups). /transfer <hostname> [<port>] [<targets>] /trigger data Modify a player's own 'trigger' scoreboard objective (used for clickable menus). /trigger <objective> [add|set <value>] /tutorialhint info Show a tutorial hint to a player (1.21.6+). /tutorialhint <hint> [<targets>] /version info Print the running Minecraft version. /version /waypoint world Manage compass-trackable waypoints (1.21.6+). /waypoint (add|remove|list|set) [...] /whitelist admin Manage the whitelist of allowed players. /whitelist (add|remove|list|on|off|reload) [<player>] /worldborder world Set or animate the world border (square boundary that pushes players inward). /worldborder (add|set|center|damage|warning|get) [...] /fetchprofile data Pre-fetch a player profile to populate UUID/skin caches (1.21.6+). /fetchprofile <player>